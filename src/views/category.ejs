<%- include('partials/header') %>

    <main>
        <h1>
            <%= categoryDetails.name %>
        </h1>

        <ul>
            <% projectDetails.projects.forEach(project=> { %>
                <li><a href="/project/<%= project.service_project_id %>">
                    <%= project.title %>
                </a>
                </li>
                <% }); %>
        </ul>

    </main>

    <%- include('partials/footer') %>